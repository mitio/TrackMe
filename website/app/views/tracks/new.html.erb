<h1>New track</h1>

<% form_for(@track) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => "track_form", :locals => {:f => f} %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', tracks_path %>